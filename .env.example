BOT_TOKEN=123456789:AAAA-your-botfather-token-here
TIMEZONE=Asia/Singapore
# DB_PATH=/app/data/birthdays.db

# Optional: voice-note birthday adds run fully locally and are free -- no
# API key needed. Only set this if you want a different model size than
# the default "base" (~75MB, fast). Bigger = more accurate, slower, more RAM.
# Options: tiny, base, small, medium, large-v3
# WHISPER_MODEL_SIZE=base
